<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no" />
    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.0.0/dist/css/bootstrap.min.css" integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" crossorigin="anonymous" />
    <!-- Custom CSS -->
    <link rel="stylesheet" href="/styles.css">
    <title>Cure Cloud</title>
    <!-- Add any additional styles or scripts if needed -->
    <style>
        body {
            background-image: url('images/adv1.jpg'); /* Add the path to your background image */
            background-size: cover; /* Cover the entire background */
            background-position: center; /* Center the background image */
            color: #333; /* Dark gray text color */
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
        }

        .header {
            background-color: #b5d6fa; /* Blue header background color */
            color: #fff; /* White header text color */
            padding: 10px;
            text-align: center;
        }

        .advertise-container {
            background-color: rgba(255, 255, 255, 0.2); /* White container background with transparency */
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            margin: 20px auto;
            padding: 20px;
            max-width: 500px;
        }

        label {
            font-weight: bold;
        }

        textarea {
            width: 100%;
            padding: 10px;
            margin-top: 5px;
            margin-bottom: 10px;
            border: 1px solid #ccc;
            border-radius: 4px;
            resize: horizontal; /* Allow vertical resizing */
            background-color: rgba(255, 255, 255, 0.2);
        }

        button[type="submit"] {
            background-color: #7799bc; /* Blue submit button background color */
            color: #fff; /* White submit button text color */
            border: none;
            padding: 10px 20px;
            border-radius: 4px;
            cursor: pointer;
            transition: background-color 0.3s;
        }

        button[type="submit"]:hover {
            background-color: #0056b3; /* Darker blue on hover */
        }

        a {
            display: block;
            text-align: center;
            margin-top: 20px;
            color: #007bff; /* Blue link color */
            text-decoration: none;
            font-weight: bold;
        }
      
    </style>
</head>
<body>
    <div class="header">
        <h1>Advertise Here</h1>
    </div>

    <div class="advertise-container">
        <form action="/advertise_here" method="post">
            <label for="advertiseText">Your Advertisement:</label>
            <textarea id="advertiseText" name="advertiseText" rows="4" cols="50" placeholder="Write your advertisement here..."></textarea>
            <br>
            <input type="hidden" name="pharmacyId" value="<%= pharmacyId %>">
            <input type="hidden" name="medicalRepresentativeId" value="<%= medicalRepresentativeId %>">
            <input type="hidden" name="drugId" value="<%= drugId %>">
            <button type="submit">Submit Advertisement</button>
        </form>
    </div>

    <a href="/">Back to Pharmacy Information</a>
</body>
</html>
